{
  "name": "authen-ledger-netlify-functions",
  "version": "1.0.0",
  "description": "Netlify functions for Authen Ledger eKYC application",
  "main": "api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:generate": "npx prisma generate",
    "build:render": "npm run db:generate && npm run build",
  "start:render": "node server.js"
  },
  "dependencies": {
    "@prisma/client": "^6.14.0",
    "express": "^5.1.0",
    "cors": "^2.8.5",
    "serverless-http": "^3.2.0"
  },
  "engines": {
    "node": "18.x"
  }
}